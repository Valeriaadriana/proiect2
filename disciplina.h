#pragma once
class Discipline
{
public:
	Discipline(const std::string & subject, const Discipline type, const std::string& specialization)

		m_Type(type);
	m_Specialization(specialization);
private:
	m_Subject(subject),
};